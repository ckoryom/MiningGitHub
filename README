MiningGitHub
-------------

This is a research project written in python which connects with the github RESTFUL api and mines commits, issues and other data from GitHub.

The main application is application.py

To run just download the project and go to a terminal and write the command:

python application.py
